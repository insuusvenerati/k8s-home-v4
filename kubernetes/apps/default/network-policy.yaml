apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: default-policy
  namespace: default
spec:
  endpointSelector: {}
  ingress:
    - fromEndpoints:
        - {}
    - fromEntities:
        - all
    - toPorts:
        - ports:
            - port: "7878"
              protocol: ANY
            - port: "5055"
              protocol: ANY
          rules:
            http: [{}]
  egress:
    - toEntities:
        - cluster
    - toEndpoints:
        - {}
    - toEntities:
        - world
    - toEndpoints:
        - matchLabels:
            io.kubernetes.pod.namespace: kube-system
            k8s-app: kube-dns
      toPorts:
        - ports:
            - port: "53"
              protocol: UDP
          rules:
            dns:
              - matchPattern: "*"
